#include "main.h"

/**
*_isupper - checking on lowercase characters
*@c:character to check
*Return 0 for anything and 1 for uppercase
*/

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z'
			return (1);
	else
	return (0);
}

